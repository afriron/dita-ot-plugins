<dummy xmlns:dita="http://dita-ot.sourceforge.net" xmlns:if="ant:if">
	<param name="textWrapping" expression="${wrap}" if:set="wrap"/>
	<param name="skipToc" expression="${skipToc}" if:set="skipToc"/>
	<param name="pbuild" expression="${pbuild}" if:set="pbuild"/>
</dummy>